[project]
name = "aegis-runtime"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "pydantic>=2.6.0",
    "psutil>=5.9.0",
    "cryptography>=43.0.0",
]

[tool.vercel]
entrypoint = "aegis.api.server:app"
